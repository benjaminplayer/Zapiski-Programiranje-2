# 1. Ignore everything
*

# 2. Allow directories (necessary so Git can look inside them)
!*/

# 3. Allow specific files globally
!*.c
!*.h
!*.md

# 4. Explicitly ignore everything inside Tests EXCEPT the runTests binary
Tests/*
!Tests/runTests

# 5. Global ignores for compiled files
*.exe
*.o