#include <stdio.h>
#include <stdlib.h>

int arr[1000000000];
int mem[1000000000];

int find(int n, int k)
{

}

int main()
{
    int n, k, r;
    scanf("%d %d %d", &n, &k, &r);

    for (int i = 0; i < n; i++)
        scanf("%d",&arr[i]);

    return 0;
}