# Runtests
Runtests is a simple program used to run the tests provided for C programs.
## Usage
To run runtests use the following command:
```bash
./runTests path_to_C_file.c path_to_test_dir
```
This will create a new directory /res, where all the outputs and inputs will be stored.
